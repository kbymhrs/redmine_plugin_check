# Installation

Copy the plugin into the Redmine plugins directory.

```bash
cp -r redmine_plugin_check plugins/
```

Restart Redmine.

No database migration is required.

The plugin is read-only and does not modify your Redmine installation.
